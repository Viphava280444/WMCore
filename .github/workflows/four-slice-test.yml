# Dispatch stub: real jobs live on branch ci-improve-exp (that branch's copy).
# Exists on master only so `--ref ci-improve-exp` is dispatchable.
name: Four slice test
on:
  workflow_dispatch:
jobs:
  pointer:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Real version on branch ci-improve-exp"
