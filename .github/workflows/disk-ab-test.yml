# Dispatch stub: real jobs live on branch disk-ab-test (that branch's copy).
# Exists on master only so `--ref disk-ab-test` is dispatchable.
name: MariaDB disk vs RAM A/B
on:
  workflow_dispatch:
jobs:
  pointer:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Real version on branch disk-ab-test; dispatch with --ref disk-ab-test"
