Originally copied from dmwm/WMCore-Jenkins @ b203d873 (main), trimmed to the
files the CI actually runs. These copies are now maintained here: they carry
this repo's variable names and wording, so they are no longer byte-identical
to that source.

TestScripts/test-wmcorepy3.sh, env_unittest.sh, pylintPRTest.sh
ContainerScripts/PullRequestTestBegin.py, PullRequestReport.py,
  IdentifyPythonFiles.py, AggregatePylint.py
templates/ (the 4 report templates)
WMCore-PR-test/setup-users.sh
